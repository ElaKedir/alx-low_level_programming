#include "main.h"
#include <stdio.h>

/**
 * _strcpy - copies one string into another
 * @dest: the string which is copies into
 * @src: the source string
 * Return: Always 0.
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (0);
}

