#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strlen - a functions that prints the length of a string
* @a: the string variable
* Return: 0 (success)
*/

int _strlen(char *s)
{
    return strlen(s);
}
