#include "main.h"
#include <string.h>

/**
* _strcmp - a function to to compare two strings based on their ascii value.
* @s1: The first string
* @s2: The second string
* Return: 0 (Success)
*/

int _strcmp(char *s1, char *s2)
{
return (strcmp(s1, s2));
}
