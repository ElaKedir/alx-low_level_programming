#include <stdlib.h>

/**
 * add - A function that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: the multiplication of the numbers
 */

int mul(int a, int b)
{       
        return (a * b);
}               

