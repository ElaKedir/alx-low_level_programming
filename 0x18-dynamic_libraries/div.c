#include <stdio.h>

/**
 * add - A function that divides two integers
 * @a: first integer
 * @b: second integer
 * Return: the division of the numbers
 */

int div(int a, int b)
{       
        return (a / b);
}               

