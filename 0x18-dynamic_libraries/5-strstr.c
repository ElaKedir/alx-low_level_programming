#include "main.h"
#include <string.h>

/**
 * _strstr - the function
 * @haystack: the source string.
 * @needle: the condition.
 * Return: 0 (success).
 */

char *_strstr(char *haystack, char *needle)
{
return (strstr(haystack, needle));
}
