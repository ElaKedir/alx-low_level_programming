#include "main.h"
#include <string.h>

/**
 * _strchr - the function to mention or find the first character in a string
 * @c: the character.
 * @s: the string.
 * Return: A pointer to the changed string.
 */

char *_strchr(char *s, char c)
{
return (strchr(s, c));
}
