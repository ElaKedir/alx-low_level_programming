#include <stdio.h>
#include "main.h"

/**
* mul - a function that multiplies two numbers.
* @a: the first digit that is to be input
* @b: the second number to be input
* Return: 0 (Success)
*/

int mul(int a, int b)
{
int result = a * b;
return (result);
}
