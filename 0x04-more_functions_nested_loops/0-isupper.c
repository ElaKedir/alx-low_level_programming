#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _isupper - returns 1 if the character is uppercase and 0 if it is not.
 * @c: the character
 * Return: 0 (Success)
 */

int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
return (0);
}
